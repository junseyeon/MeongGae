<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meong_gae">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".Global"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ModifyPwd"></activity>
        <activity android:name=".ModifyEmail" />
        <activity android:name=".ModifyProfile" />
        <activity android:name=".DogRegistration" />
        <activity
            android:name=".MypageUserSetting"
            android:exported="false" />
        <activity
            android:name=".ui.reserve.ReserveConfirmActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".ReserveConfirm" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_reserve_confirm" -->
        <!-- android:theme="@style/NoActionBar" /> &lt;!&ndash; AppCompat.NoActionBar로 하면 툴바?액션바? 안보임 &ndash;&gt; -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:label="@string/title_activity_home" />
        <activity android:name=".ui.reserve.ReserveDetail" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SignupCheckActivity" />
        <activity android:name=".SignupActivity" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>